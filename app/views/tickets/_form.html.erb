<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :requestNumber, style: "display: block" %>
    <%= form.text_area :requestNumber %>
  </div>

  <div>
    <%= form.label :sequenceNumber, style: "display: block" %>
    <%= form.number_field :sequenceNumber %>
  </div>

  <div>
    <%= form.label :requestType, style: "display: block" %>
    <%= form.text_area :requestType %>
  </div>

  <div>
    <%= form.label :requestAction, style: "display: block" %>
    <%= form.text_area :requestAction %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
